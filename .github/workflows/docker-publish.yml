# 1. 切换到 gbf 分支
git checkout gbf

# 2. 打开原工作流文件（注意路径！）
code .github/workflows/docker-publish.yml

# 3. 修改触发分支（只改这里！）
on:
  push:
    branches:
      - main
      - gbf  # ← 添加这一行（关键！）
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - gbf  # ← 添加这一行
  workflow_dispatch:
